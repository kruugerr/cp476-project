# macOS
.DS_Store
**/.DS_Store

# Editor / OS junk
Thumbs.db
swp
.vscode/

# Node (frontend tooling, if any added later)
node_modules/

# Environment variables (DB password, JWT secret — never commit)
.env
.env.local

# Logs
npm-debug.log*
*.log